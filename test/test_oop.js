const assert = require('chai').assert;
const animimalIndex = require('../index');


describe("To create a class ", ()=> {

    describe("should return 0 for an empty array", ()=> {
        it("should return string", ()=> { 
            assert.equal(aritGeo([]), 0);
        });   
    });
});